{
  "name": "optasy",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-web.mjs",
    "build:live": "node scripts/build-web.mjs --require-live",
    "refresh": "node scripts/refresh-data.mjs && node scripts/contribution-data.mjs --optional",
    "dev": "python3 -m http.server 8786 --bind 127.0.0.1 --directory web"
  }
}
